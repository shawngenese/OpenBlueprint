---
description: Review recent code changes against AGENTS.md rules
agent: build
---
1. Run `git status` and `git diff` to view recent modifications.
2. Verify that changes follow the rules in `AGENTS.md`.
3. Report any missing types, broken imports, or rule violations.